# 隐性流派构筑设计

## 核心原则

不要让玩家在开局选择流派。玩家只选择角色，流派应该在探索过程中由拿牌、买牌、拿遗物慢慢长出来。

这更接近《杀戮尖塔》的体验：玩家不是先选择“力量流、腐化流、壁垒流”，而是在奖励选择中逐渐被手里的牌、遗物和风险推向某个方向。系统可以顺着玩家已经做出的选择稍微提高相关奖励权重，但不能把其它方向锁死。

## 结构

角色仍然是唯一显式选择：

- 勇者战士
- 萝莉魔导士
- 精灵弓箭手

流派是隐藏构筑方向，通过卡牌和遗物上的 `buildTags` 标记，或通过卡牌词条自动推断。

## 同步口径

本文件记录设计契约，不作为卡牌、遗物或运行时效果的最终数据源。

- 当前权威数据：`src/data/cards.js`、`src/data/relics.js`、`src/data/characters.js`。
- 当前权威运行时：`questers_demo_v0.99.html`。
- 当前权威模拟逻辑：`tools/simulate_build_balance.mjs` 与 `tools/run_full_playtest_suite.mjs`。
- 修改卡牌、遗物、史诗核心或初始牌组时，必须同时检查本文件中对应的流派契约、整理规则、史诗核心与遗物规则。
- 历史测试报告只代表生成当天的快照；如果报告与源数据不一致，以源数据和重新运行的测试结果为准。

## 构筑方向

### 勇者战士

- `oathblade`：圣剑反击。护盾、反击、圣剑、重击，是当前默认方向。
- `execution`：处刑连斩。易伤、穿甲、连击，减少对堆护盾的依赖，打短窗口爆发。
- `bloodoath`：血誓魔剑。承受实际失血来淬炼魔剑，并用已损生命、出血与吸血在危险血线中换取爆发。

血誓魔剑的玩家可见规则：

1. 核心张力是“扣血换输出”和“回血保命会降低已损生命收益”之间的取舍。
2. 自伤牌不能把玩家直接扣死；只有实际失去生命时，才触发魔剑成长与相关遗物收益。
3. 敌方攻击造成的实际生命损失也能触发魔剑成长，护盾吸收或没有真实掉血时不触发。
4. `魔剑` 基础伤害很低，靠自身实际失去生命或本牌击杀敌人获得永久伤害。
5. 血誓魔剑不用护盾或庇护补生存，主要依靠吸血、治疗、出血/放血、虚弱/眩晕和低血量爆发继续进攻。

当前血誓魔剑仍保留 `bloodOathCost`、`bloodOathMissingRatio`、`bloodDebtBleed`、`bloodDebtWeak`、`bloodDebtStun` 等内部字段名，但这些字段只表示“自伤代价、已损生命收益、附加异常”，不再代表旧版血债清算玩法。新增卡面不要写血债、偿债、清算倒计时等旧概念。

### 萝莉魔导士

- `chant`：星火咏唱。积累咏唱，用爆发牌收束。
- `mirror`：镜像回路。先打关键牌，再用回响自回放或复刻上一张非复刻牌。
- `calamity`：灾厄术士。诅咒、虚弱、易伤、燃烧等多状态压制。

### 精灵弓箭手

- `gale`：疾风连射。风势、蓄力、自然、追击，是当前默认方向。
- `venom`：猎毒陷袭。剧毒、出血、放血，延迟爆发。
- `exile`：放逐游侠。放逐、回收、流动伤害，管理牌区循环。

## 奖励生成规则

- 卡牌奖励与商店只从人工定义的职业牌池、中立牌池和史诗核心池中抽取。
- 禁止运行时随机拼接费用、数值、名称或词条来生成新牌。
- 固定牌池暂时无法满足槽位条件时，放宽流派或稀有度筛选继续从固定牌池补位。
- 旧存档中已经存在的程序生成牌不主动删除，但后续奖励不再产生新牌。

卡牌奖励三选一尽量形成这种结构：

1. 一张顺着当前牌组主要方向的牌。
2. 一张普通随机牌或通用牌。
3. 一张可能打开其它方向的牌。

第一场战斗后也使用普通奖励，不再固定展示三张分别代表三个方向的牌。玩家不会在开局或首战后做一次伪装成奖励的流派确认。

如果牌组里出现“有出血但没有放血”或“有放血但没有出血”，优先补一张搭桥牌，因为这会直接改善牌组可玩性。

## 权重而不是锁池

构筑方向只影响权重：

- 主方向牌略微更容易出现，但不会压过普通牌与转向牌。
- 已拥有的相关遗物可以温和提高对应方向权重，不再直接锁定主方向。
- 史诗核心会参考当前牌组倾向。
- 其它方向仍保留出现概率，允许转型。

不要把牌池拆成开局固定流派，也不要在 `state.player` 里保存 `archetypeId`。

## 当前落地

- `src/data/cards.js` 定义 `BUILD_DIRECTIONS` 和 `CARD_BUILD_TAGS_BY_ID`。
- `src/data/relics.js` 定义 `RELIC_BUILD_TAGS_BY_ID`。
- `questers_demo_v0.99.html` 根据当前牌组计算构筑倾向，并用于卡牌、史诗核心、奇珍奖励和商店补货。
- 选择角色后直接生成地图，不再展示开局核心遗物选择，也不再改造初始牌。
- 起始牌只以较低权重参与构筑倾向计算；拿到新的方向牌后，奖励才开始提供温和的同向偏好。
- 两个方向分数并列或接近时不判定主方向，避免一张桥接牌按内部顺序替玩家锁定路线。

后续新增新流派内容时，优先做卡牌和遗物内容，不要做新的开局选择界面。

## 卡牌整理规则

整理卡牌时优先看“玩家这回合在做什么”，不要只看名字或美术。

- 词条系统是卡面解释词典，不是卡牌拼装配料表。只有当卡牌实际造成对应效果时，才把词条放到卡面上；例如“造成 8 点伤害并附加易伤”需要 `易伤` 说明，而不是为了凑流派把多个词条硬拼上去。
- 职业卡只使用本职业语汇。通用解释词可以少量共享，但不能因为词条系统存在，就让法师拿弓手毒血、弓手拿法师回响或燃烧、血誓牌拿回圣剑护盾。
- 通用卡牌不是第四套流派牌池，只保留少量三职业都能自然使用的基础动作。通用卡池上限为 8 张，允许词条限于抽牌、充能、保留、重置、销毁、庇护、治愈、易伤、虚弱、眩晕。
- 勇者语汇：魔剑、血誓、吸血、出血、放血、重击、穿甲、圣剑、反击、庇护、易伤、虚弱、眩晕等，围绕近战、失血取舍和短窗口爆发。
- 法师语汇：咏唱、爆发、回响、复刻、诅咒、燃烧、易伤、虚弱、眩晕等，围绕施法节奏、镜像与灾厄压制；灾厄不使用弓手的剧毒、出血、放血。
- 弓手语汇：追击、蓄力、自然、闪避、剧毒、出血、放血、放逐、回收、穿甲等，围绕射击节奏、陷阱毒血和牌区循环；疾风不使用法师回响或燃烧。
- 卡牌文本只写真实效果，不写“适合某流派”“用于某窗口”这类构筑说明。构筑提示可以留在设计文档或调试统计里，不出现在卡面。
- 卡面可以由最终效果自动生成，但生成结果必须是完整规则句，不能只是把词条名拼成列表。若一张牌只靠词条列表才能表达，说明这张牌还没有设计完成。
- 普通牌只保留一个中心动作；如果带 `销毁`，它只能作为一次性代价附在一个中心动作之后。
- 稀有牌原则上仍只有一个中心动作，可以用数值、费用、保留时机或目标条件体现差异，不靠多个词条拼接成“万金油牌”。
- 史诗牌和史诗核心可以有两个关键词或专属分支，但效果必须服务同一个主题；不要把抽牌、充能、庇护、状态、回收等多个独立收益塞进同一张牌。
- 非核心卡如果已经有明确关键词，不再保留隐藏的抽牌、充能、庇护、保留或治疗副效果；没有关键词的功能牌最多保留一个直接效果。
- `攻击` 牌必须有明确伤害值；没有伤害值的牌不要标成攻击牌。
- 卡牌基础数值采用费用溢价：1 费保持原值，2 费按约 `1.35` 倍结算，3 费按约 `1.70` 倍结算。目标是让平均每点能量收益随费用上升，而高费牌仍承担更难塞进回合的风险。
- 高费能力牌也必须获得溢价：2 费以上的抽牌、咏唱和负面状态层数随费用增长；能力牌升级强化效果，不再通过直接降费制造低费循环件。
- 费用重标定参考《杀戮尖塔》的“每回合出牌数”而不是照搬 3 点能量：讨魔旅人的基础能量保持勇者 5、弓手 5、法师 6，目标是常规回合约打出 3-4 张牌。基础攻击、防御和单一弱效果多为 1 费；抽牌、回收、重置、复刻、回响、充能、眩晕、免费启动和放逐循环等会压缩手牌节奏的效果，需要提高到 1-2 费并给足单牌收益；0 费只留给低数值一次性牌或明确有代价的牌。
- 奖励牌必须以初始牌为最低参照。新攻击牌若同费伤害低于基础攻击，必须在当前卡面或常规无核心环境下提供足够清楚的补偿，如状态、抽牌、回收、爆发倍率、穿甲、流动伤害或防线收益；否则玩家没有理由拿它。高费奖励牌不能只把两张基础牌的收益合并成一张，还要给出额外构筑方向或明显数值溢价。
- `连射`、`多段`和风势追射的额外段只重复主要结算，不重复触发 `自然`、`咏唱`、抽牌和职业联动等启动收益。
- `穿甲`、`重击`、`连击`、`多段`、`连射`、`吸血` 这类伤害修饰词条，必须出现在会造成伤害的攻击牌上。除非史诗核心有专属运行时分支明确造成伤害，否则不要挂在纯能力牌或纯防御牌上。
- `反击` 不再作为低费通用防守词条。正常卡牌与史诗核心若带 `反击`，费用至少为 3；低费防守牌改用 `庇护`、`保留`、`荆棘`、`充能` 等较温和的词条。
- `反击` 的运行时效果是招架下一次敌方主体攻击伤害的 50%，并按招架值反击；姿态不可叠加。
- 勇者的 `bloodoath` 牌不要再强行挂 `圣剑`，也不要加入防御牌、护盾或庇护，否则会重新回到圣剑收益。
- 血誓魔剑卡池必须同时保有自伤启动、已损生命收益、魔剑成长、回血回稳和高风险窗口五类职责；新增牌不能只写“低数值攻击加血誓标签”。
- 带自伤代价的血誓牌应同时提供伤害、出血、放血、抽牌或控制中的至少一项；回血牌在高血量时也应保留基础攻击、抽牌、保留或控制价值。
- 魔剑相关效果只在实际生命损失或本牌击杀时成长，不能用“生命已经为 1 但继续打自伤牌”刷永久伤害。
- 不是所有血誓魔剑遗物和史诗核心都要服务魔剑成长。血誓魔剑池仍需要保留失血增伤、吸血回稳、出血/放血和低血量收益等非魔剑支点。
- 勇者的 `execution` 牌应更偏 `易伤`、`穿甲`、`连击`、`重击`，减少护盾和反击依赖。
- 法师的 `calamity` 牌不要每张都混 `咏唱`，否则会被默认爆发路线吞掉；它应该靠法师语汇内的负面状态建立压力。
- 法师的 `咏唱` 是通用资源，不是 `chant` 流派独占词条；`mirror` 和 `calamity` 可以少量使用咏唱，但身份必须来自牌序或负面状态。
- 法师的 `mirror` 牌必须能回答“复制/回放哪张牌”：`回响` 是本牌自回放，`复刻` 是上一张非复刻牌。避免把回响、复刻、抽牌、充能、保留堆成看不出主动作的牌。
- 弓手的 `venom` 和 `exile` 牌不要每张都混 `连射`，否则疾风路线会吃掉所有方向；毒血牌靠层数和引爆，放逐牌靠牌区循环。
- 可以保留少量桥接牌，但桥接牌要有明确作用，例如让玩家从当前方向自然转向，而不是所有词条都堆在一张牌上。

勇者当前的桥接原则：

- `圣痕破势`连接圣剑与处刑：利用护盾收益进攻，同时留下易伤窗口。
- `誓血缚阵`不再连接处刑：它会启动血誓自伤与眩晕，应只归属血誓魔剑。
- `刑架削锋`连接处刑与血誓魔剑：用重击提供收束，用虚弱为无护盾血誓牌组压低失败代价；它不直接启动自伤。
- 暂不设置圣剑与血誓魔剑的直接护盾桥，因为这会破坏血誓魔剑不依赖护盾的身份。

## 史诗核心与遗物整理规则

史诗核心和遗物不应该把玩家重新拉回默认玩法。它们应该放大玩家已经长出来的方向，并提供少量桥接空间。

- 勇者的处刑核心看 `易伤`、`穿甲`、`连击`、`重击`，不要再吃护盾和荆棘收益。
- 勇者的血誓魔剑核心围绕已损生命、吸血回稳、出血/放血或少量魔剑成长提供爆发；可以有一部分核心和遗物加入魔剑成长，但不能把全部血誓内容都改成魔剑专属。
- 法师的灾厄核心看负面状态数量和状态链，不要把所有灾厄收益都转成 `咏唱`。
- 弓手的毒血核心看 `剧毒`、`出血`、`放血`，不要靠风势抽牌或连射次数来证明价值。
- 弓手的放逐核心看放逐区规模、回收和穿甲，不要再用风势双发作为主要奖励。
- 遗物描述必须和实际运行时一致。若文案写了首次免伤、低血护盾等效果，就要在战斗逻辑中实现；没有实现的效果不要写进描述。
- 遗物可以作为桥接工具，例如弃牌、抽牌、穿甲、能力减费，但文案上要说明“补强/转向”，不要把它写成某个固定流派选择。

## 扩容目标

当前扩容目标是让每个角色的三个隐藏构筑方向都至少拥有：

- 职业卡牌 20 张。
- 史诗核心 8 个。
- 职业遗物 12 个。

通用池不按流派补强，而是作为少量跨职业基础工具：

- 通用卡牌最多 8 张。
- 通用遗物至少 12 个。

允许桥接牌和桥接遗物被多个方向重复计数，但新增内容要尽量避免只有数值差异。优先用不同动词区分：启动、压缩、控制、保留、抽牌、回收、放逐、引爆、回血、短窗口爆发、持续状态压制。

每个隐藏构筑方向至少有一个普通流程中可获得的“概率遗物”，通过 `RELIC_CARD_REWARD_BONUS_BY_ID` 接入奖励权重。它只提供温和引导，不负责开局选流派、改造初始牌或锁定奖励池。

新增史诗核心若没有专属 `specialId` 运行时分支，会通过通用兜底按自身词条正常结算，避免出现只有描述但没有效果的核心卡。

## 当前达成数量

以下统计去除通用池，按当前开放构筑检查中的可选职业牌候选、显式史诗核心归属和遗物 `buildTags` 统计；桥接内容会分别计入对应方向。运行时仍会通过词条推断补充奖励权重，所以这里用于检查文档和牌池是否明显脱节，不作为锁池依据。

| 角色 | 流派 | 可选职业牌候选 | 史诗核心 | 遗物 | 概率遗物 |
|---|---:|---:|---:|---:|---:|
| 勇者战士 | 圣剑反击 | 18 | 8 | 11 | 1 |
| 勇者战士 | 处刑连斩 | 12 | 8 | 12 | 1 |
| 勇者战士 | 血誓魔剑 | 20 | 8 | 10 | 1 |
| 萝莉魔导士 | 星火咏唱 | 14 | 8 | 11 | 1 |
| 萝莉魔导士 | 镜像回路 | 12 | 8 | 11 | 1 |
| 萝莉魔导士 | 灾厄术士 | 14 | 8 | 11 | 1 |
| 精灵弓箭手 | 疾风连射 | 17 | 8 | 11 | 1 |
| 精灵弓箭手 | 猎毒陷袭 | 17 | 8 | 9 | 1 |
| 精灵弓箭手 | 放逐游侠 | 13 | 8 | 11 | 1 |

通用池当前为 8 张通用卡牌、13 个通用遗物，其中包含对精英增伤和对首领增伤两个通用路线遗物。
