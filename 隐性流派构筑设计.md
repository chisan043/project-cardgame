# 隐性流派构筑设计

## 核心原则

不要让玩家在开局选择流派。玩家只选择角色，流派应该在探索过程中由拿牌、买牌、拿遗物慢慢长出来。

这更接近《杀戮尖塔》的体验：玩家不是先选择“力量流、腐化流、壁垒流”，而是在奖励选择中逐渐被手里的牌、遗物和风险推向某个方向。系统可以顺着玩家已经做出的选择稍微提高相关奖励权重，但不能把其它方向锁死。

## 结构

角色仍然是唯一显式选择：

- 勇者战士
- 萝莉魔导士
- 精灵弓箭手

流派是隐藏构筑方向，通过卡牌和遗物上的 `buildTags` 标记，或通过卡牌词条自动推断。

## 构筑方向

### 勇者战士

- `oathblade`：圣剑反击。护盾、反击、圣剑、重击，是当前默认方向。
- `execution`：处刑连斩。易伤、穿甲、连击，减少对堆护盾的依赖，打短窗口爆发。
- `bloodoath`：血誓狂战。先借入血债放大攻击，再用吸血、放血与主动偿债在回合结束前清偿；未偿部分无视护盾结算为生命损失。

血债的固定结算顺序：

1. 借债牌获得血债，但不立刻失去生命。
2. 只有明确写有血债倍率的牌才按当前血债追加伤害，血债不提供全局自动增伤。
3. 吸血先偿还血债，剩余数值才回复生命；指定放血牌和偿债牌也能清偿。
4. 回合结束时，未偿血债无视护盾与减伤结算，且最多把生命降到 1 点。
5. 血誓不用护盾或庇护补生存，主要依靠虚弱、眩晕争取窗口，并靠完全清偿后的回血继续进攻。

### 萝莉魔导士

- `chant`：星火咏唱。积累咏唱，用爆发牌收束。
- `mirror`：镜像回路。回响、复刻、复制和出牌顺序。
- `calamity`：灾厄术士。诅咒、虚弱、易伤、燃烧等多状态压制。

### 精灵弓箭手

- `gale`：疾风连射。风势、蓄力、自然、连射，是当前默认方向。
- `venom`：猎毒陷袭。剧毒、出血、放血，延迟爆发。
- `exile`：放逐游侠。放逐、轮回、拾遗、招魂，管理牌区循环。

## 奖励生成规则

- 卡牌奖励与商店只从人工定义的职业牌池、中立牌池和史诗核心池中抽取。
- 禁止运行时随机拼接费用、数值、名称或词条来生成新牌。
- 固定牌池暂时无法满足槽位条件时，放宽流派或稀有度筛选继续从固定牌池补位。
- 旧存档中已经存在的程序生成牌不主动删除，但后续奖励不再产生新牌。

卡牌奖励三选一尽量形成这种结构：

1. 一张顺着当前牌组主要方向的牌。
2. 一张普通随机牌或通用牌。
3. 一张可能打开其它方向的牌。

如果牌组里出现“有出血但没有放血”或“有放血但没有出血”，优先补一张搭桥牌，因为这会直接改善牌组可玩性。

## 权重而不是锁池

构筑方向只影响权重：

- 主方向牌更容易出现。
- 相关遗物更容易出现。
- 史诗核心会参考当前牌组倾向。
- 其它方向仍保留出现概率，允许转型。

不要把牌池拆成开局固定流派，也不要在 `state.player` 里保存 `archetypeId`。

## 当前落地

- `src/data/cards.js` 定义 `BUILD_DIRECTIONS` 和 `CARD_BUILD_TAGS_BY_ID`。
- `src/data/relics.js` 定义 `RELIC_BUILD_TAGS_BY_ID`。
- `questers_demo_v0.99.html` 根据当前牌组计算构筑倾向，并用于卡牌、史诗核心、奇珍奖励和商店补货。

后续新增新流派内容时，优先做卡牌和遗物内容，不要做新的开局选择界面。

## 卡牌整理规则

整理卡牌时优先看“玩家这回合在做什么”，不要只看名字或美术。

- 卡牌文本只写真实效果，不写“适合某流派”“用于某窗口”这类构筑说明。构筑提示可以留在设计文档或调试统计里，不出现在卡面。
- `攻击` 牌必须有明确伤害值；没有伤害值的牌不要标成攻击牌。
- 卡牌基础数值采用费用溢价：1 费保持原值，2 费按约 `1.35` 倍结算，3 费按约 `1.70` 倍结算。目标是让平均每点能量收益随费用上升，而高费牌仍承担更难塞进回合的风险。
- 高费能力牌也必须获得溢价：2 费以上的抽牌、咏唱和负面状态层数随费用增长；能力牌升级强化效果，不再通过直接降费制造低费循环件。
- `连射`、`多段`和风势追射的额外段只重复主要结算，不重复触发 `自然`、`咏唱`、抽牌和职业联动等启动收益。
- `穿甲`、`重击`、`连击`、`多段`、`连射`、`吸血` 这类伤害修饰词条，必须出现在会造成伤害的攻击牌上。除非史诗核心有专属运行时分支明确造成伤害，否则不要挂在纯能力牌或纯防御牌上。
- `反击` 不再作为低费通用防守词条。正常卡牌与史诗核心若带 `反击`，费用至少为 3；低费防守牌改用 `庇护`、`保留`、`荆棘`、`充能` 等较温和的词条。
- `反击` 的运行时效果是招架下一次敌方主体攻击伤害的 50%，并按招架值反击；姿态不可叠加。
- 勇者的 `bloodoath` 牌不要再强行挂 `圣剑`，也不要加入防御牌、护盾或庇护，否则会重新回到圣剑收益。
- 血誓卡池必须同时保有借债、血债伤害、偿债和高风险窗口四类职责；新增牌不能只写“低数值攻击加血誓标签”。
- 血誓的借债牌应同时提供伤害、出血、抽牌或控制中的至少一项；偿债牌在无债时也应保留基础攻击、抽牌、保留或控制价值。
- 勇者的 `execution` 牌应更偏 `易伤`、`穿甲`、`连击`、`重击`，减少护盾和反击依赖。
- 法师的 `calamity` 牌不要每张都混 `咏唱`，否则会被默认爆发路线吞掉；它应该靠多种负面状态建立压力。
- 弓手的 `venom` 和 `exile` 牌不要每张都混 `连射`，否则疾风路线会吃掉所有方向；毒血牌靠层数和引爆，放逐牌靠牌区循环。
- 可以保留少量桥接牌，但桥接牌要有明确作用，例如让玩家从当前方向自然转向，而不是所有词条都堆在一张牌上。

## 史诗核心与遗物整理规则

史诗核心和遗物不应该把玩家重新拉回默认玩法。它们应该放大玩家已经长出来的方向，并提供少量桥接空间。

- 勇者的处刑核心看 `易伤`、`穿甲`、`连击`、`重击`，不要再吃护盾和荆棘收益。
- 勇者的血誓核心消耗当前血债形成爆发，再以吸血完成清偿与回血；不要回退到即时扣血换永久攻击的旧闭环。
- 法师的灾厄核心看负面状态数量和状态链，不要把所有灾厄收益都转成 `咏唱`。
- 弓手的毒血核心看 `剧毒`、`出血`、`放血`，不要靠风势抽牌或连射次数来证明价值。
- 弓手的放逐核心看放逐区规模、回收和穿甲，不要再用风势双发作为主要奖励。
- 遗物描述必须和实际运行时一致。若文案写了首次免伤、低血护盾等效果，就要在战斗逻辑中实现；没有实现的效果不要写进描述。
- 遗物可以作为桥接工具，例如弃牌、抽牌、穿甲、能力减费，但文案上要说明“补强/转向”，不要把它写成某个固定流派选择。

## 扩容目标

当前扩容目标是让每个角色的三个隐藏构筑方向都至少拥有：

- 职业卡牌 20 张。
- 史诗核心 8 个。
- 职业遗物 12 个。

通用池也按同样思路补强：

- 通用卡牌至少 20 张。
- 通用遗物至少 12 个。

允许桥接牌和桥接遗物被多个方向重复计数，但新增内容要尽量避免只有数值差异。优先用不同动词区分：启动、压缩、控制、保留、抽牌、回收、放逐、引爆、回血、短窗口爆发、持续状态压制。

每个隐藏构筑方向至少有一个“概率遗物”，通过 `RELIC_CARD_REWARD_BONUS_BY_ID` 接入奖励权重。拿到对应遗物后，该方向卡牌和史诗核心会更容易在奖励与商栈补货中出现。

新增史诗核心若没有专属 `specialId` 运行时分支，会通过通用兜底按自身词条正常结算，避免出现只有描述但没有效果的核心卡。

## 当前达成数量

以下统计去除通用池，只按三个角色各自的隐藏构筑方向统计；双流派桥接内容会分别计入对应方向。

| 角色 | 流派 | 卡牌 | 史诗核心 | 遗物 | 概率遗物 |
|---|---:|---:|---:|---:|---:|
| 勇者战士 | 圣剑反击 | 21 | 8 | 12 | 1 |
| 勇者战士 | 处刑连斩 | 21 | 8 | 12 | 1 |
| 勇者战士 | 血誓狂战 | 20 | 8 | 12 | 1 |
| 萝莉魔导士 | 星火咏唱 | 20 | 8 | 12 | 1 |
| 萝莉魔导士 | 镜像回路 | 20 | 8 | 12 | 1 |
| 萝莉魔导士 | 灾厄术士 | 20 | 8 | 12 | 1 |
| 精灵弓箭手 | 疾风连射 | 20 | 8 | 12 | 1 |
| 精灵弓箭手 | 猎毒陷袭 | 20 | 8 | 12 | 1 |
| 精灵弓箭手 | 放逐游侠 | 20 | 8 | 13 | 1 |

通用池当前为 20 张通用卡牌、12 个通用遗物。
