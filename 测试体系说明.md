# Questers 测试体系说明

## 目标

这套测试体系用于长期回答五个问题：

- 游戏是否好玩：用单局评分、跑局时长、死亡原因、负反馈敌人和异常项观察。
- 难度曲线是否合理：用楼层死亡热区、章节死亡率、Boss 通过率、平均损血观察。
- 构筑是否多样：用最终流派、核心卡/遗物获得、卡牌选择率、组合胜率观察。
- 随机性是否公平：用混合路线、奖励出现/选择、获得楼层和通关率差异观察。
- 失败是否让玩家觉得可以改进：用死亡楼层、死亡原因、敌人负反馈和主观评分观察。

## 入口

发布前基础检查：

```bash
node tools/run_release_checks.mjs
```

这条命令用于每次提交前的快速健康检查，会验证资源审计、卡图/遗物 registry、程序化卡牌禁用和开局构筑流。
它只把 asset audit 临时报告写到系统临时目录，不会改写 `tools/playtest_reports/` 或 tracked 报告。

完整跑局体验与平衡测试：

```bash
node tools/run_full_playtest_suite.mjs --runs-per-build 40 --novice-runs-per-build 12 --seed 20260614
```

默认口径：80% 跑局按正常玩家的多流派混合构筑模拟，20% 跑局作为纯流派对照。可以用 `--mixed-run-share` 调整比例。

默认输出目录：`tools/playtest_reports/`。

主要输出：

- `完整跑局体验与平衡测试报告.md`
- `full_playtest_report.json`
- `single_runs.csv`
- `card_stats.csv`
- `relic_stats.csv`
- `enemy_stats.csv`
- `floor_heatmap.csv`
- `route_stats.csv`
- `combo_stats.csv`
- `anomalies.csv`

史诗核心逐张审计：

```bash
node tools/analyze_epic_core_balance.mjs --runs 120 --seed 20260616 --out-dir /tmp/questers_epic_core_balance
```

这条命令会把每张史诗核心强制放进对应构筑测试，分别对比“无核心纯卡、单核心无遗物、成熟遗物无核心、核心加成熟遗物”四组场景。它用于补足完整跑局测试的覆盖盲区：完整跑局里的 Boss 奖励会按构筑权重给候选牌，成熟构筑模板也只使用每个流派的代表核心，因此并不能保证每张史诗核心都被实际验证。

默认输出到 `/tmp/questers_epic_core_balance/`，包括：

- `epic_core_balance_report.md`
- `epic_core_balance_report.json`
- `epic_core_balance.csv`
- `epic_core_anomalies.csv`

## 数据口径

### 单局记录

`single_runs.csv` 记录角色、难度、玩家画像、构筑策略、目标流派、副流派、是否通关、死亡楼层、死亡原因、最终流派、构筑混合摘要、核心卡、核心遗物、总时长、击败精英数、商店次数、删卡数、升级数和模拟主观评分。

构筑策略分为：

- 混合构筑：默认 80% 样本。奖励、商店、遗物和核心牌会围绕主副两个方向加权，模拟玩家正常游玩中大多不会只拿单一流派牌。
- 纯流派对照：默认 20% 样本。保留它是为了观察某一方向的上限和异常，但不作为正常体验的唯一结论。

### 楼层/关卡难度

`floor_heatmap.csv` 统计每层到达数、死亡率、战斗胜率、平均剩余血量、平均战斗回合和平均损血。

报告中的章节表统计每章死亡率、Boss 通过率、精英击杀率、平均剩余血量、平均战斗回合和平均损血。

### 卡牌数据

`card_stats.csv` 统计出现次数、选择次数、选择率、升级率、抽到后使用率、选后通关率、平均贡献值和模拟玩家评分。

平均贡献值不是精确伤害归因，而是根据卡牌使用次数、数值、抽牌、充能、保护、治疗、循环等效果估算的测试指标。用于排序和预警，不作为最终设计结论。

### 遗物数据

`relic_stats.csv` 统计获得次数、平均获得楼层、获得后通关率、触发次数、每次获得后的平均触发、平均收益和模拟玩家评分。

`combo_stats.csv` 统计遗物+卡牌、遗物+遗物的组合样本数和组合胜率。

### 敌人数据

`enemy_stats.csv` 统计遭遇次数、胜率、死亡率、平均损血、平均战斗回合和负反馈次数。

负反馈来自高损血、快速死亡等模拟信号，用于提示需要人工复盘的敌人。

### 路线选择

`route_stats.csv` 统计普通怪、精英、商店、事件、休息点、宝箱、Boss 的出现次数、选择次数、选择率和平均收益。

### 构筑混合口径

完整跑局报告中的总体通关率优先代表“80% 混合 + 20% 纯流派对照”的正常玩家样本。判断是否好玩、难度是否合理、失败是否可改进时，优先看总体、混合构筑分组和最终流派分布；单一流派结果只用于定位某条路线的极端上限或下限。

`analyze_practical_balance.mjs` 的混合拿牌场景也按这个口径解释：混合牌组中目标流派与其它流派牌接近各半，并在报告里提供“正常无核心加权”结果。

## 关键阈值

- 普通难度熟练玩家通关率目标：50%-75%。
- 新手到达第一章 Boss 目标：至少 65%。
- 完整跑局默认混合构筑样本占比：80%；纯流派只作为对照组。
- 废卡比例目标：低于 20%。
- 单场战斗超过 18 回合：标记为单局过长风险。
- 单场回血超过最大生命 2.2 倍：标记为无限回血风险。
- 敌人遭遇死亡率超过 45%：标记为过难敌人。
- 楼层死亡率超过 22%：标记为死亡热区。
- 非固定路线节点选择率超过 55%：标记为路线单一风险。

## 使用建议

先看 `anomalies.csv`，再回到对应报表定位原因。

## 文档同步规则

- `隐性流派构筑设计.md` 只记录设计契约，不替代源数据。
- `初始牌组测试报告.md`、`实战缺核心平衡测试报告.md`、`卡牌构筑平衡调整验证报告.md` 都是快照报告；修改卡牌、遗物、史诗核心、初始牌组、费用体系、基础能量、奖励牌基线或流派名后，应重新运行对应工具，再更新报告日期、种子、关键表格和同步说明。
- 当前效果以 `src/data/cards.js`、`src/data/relics.js`、`src/data/characters.js`、`questers_demo_v0.99.html` 和模拟器为准。文档与源数据冲突时，先修正文档或重跑报告，不要按旧报告继续设计。
- 临时探索测试优先输出到 `/tmp`；只有当用户明确需要沉淀当前版本结论时，才更新 tracked 报告，避免仓库里堆积大量过期测试文件。

调数值后至少重跑：

```bash
node tools/run_release_checks.mjs
node tools/run_full_playtest_suite.mjs --runs-per-build 40 --novice-runs-per-build 12 --seed 20260614
node tools/analyze_practical_balance.mjs --runs 800 --seed 20260614
node tools/analyze_epic_core_balance.mjs --runs 120 --seed 20260616 --out-dir /tmp/questers_epic_core_balance
```

如果是在判断整体体验，建议把完整跑局样本提高到：

```bash
node tools/run_full_playtest_suite.mjs --runs-per-build 160 --novice-runs-per-build 40 --seed 20260614
```

如果改了具体卡牌或遗物，再补跑卡牌选择率、构筑诊断和相关浏览器路径验证。
